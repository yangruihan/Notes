<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.studyjams.s1.sj23.yangruihan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntroHistoryActivity" />
        <activity android:name=".IntroProductActivity" />
        <activity android:name=".IntroCultureActivity" />
        <activity android:name=".IntroDisputeActivity" />
        <activity android:name=".IntroFaultActivity"></activity>
    </application>

</manifest>